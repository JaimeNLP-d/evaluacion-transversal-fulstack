server:
  #servlet:context-path: /generos/api/    #ocasiona fallo!
  port: 8085

spring:
  application:
    name: grupoMicroservice
  datasource:
    url: jdbc:mysql://localhost:3306/db_musica_04
    username: root
    password: ''
  jpa:
    hibernate:
      ddl-auto: update
      #ddl-auto: create-drop   #Para Re-hacer la tabla
    show-sql: 'true'
    properties:
      hibernate:
        format_sql: 'true'
        dialect: org.hibernate.dialect.MySQLDialect
  flyway:
    enabled: true
    #repair: true     #ocasiona fallo!
    locations: classpath:db/migration

app:
  jwt:
    secret: mi-clave-jlp-18394872519930505mie-2026
    expiration-ms: 600000000

logging:
  level:
    root: INFO
    jlp:
      musica03:
        grupoMicroservice: INFO

